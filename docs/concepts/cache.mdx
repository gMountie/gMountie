---
title: Cache & consistency
sidebar_label: Cache & consistency
description: How the client-side cache works, what it stores, and why writes are still safe — push-invalidated, close-to-open consistent.
---

# Cache & consistency

**The client decorates the gRPC backend with a two-tier cache.** On a hit, the FUSE op returns without crossing the wire. On a miss, the wire call runs and seeds the cache. The server pushes invalidations so multiple clients converge without polling.

The cache is **two-tier**: a fast in-memory tier and a disk-persistent tier (under `cache.path`, default `~/.cache/gmountie`) that survives a remount. The two tiers have independent byte budgets — `cache.memory_max_bytes` (default 256 MiB) and `cache.disk_max_bytes` (default 10 GiB).

## What's cached

The attr, dir, and data sub-caches share the byte budgets above:

| Sub-cache | Holds                                | TTL key                  | Default |
| --------- | ------------------------------------ | ------------------------ | ------- |
| **attr**  | `getattr` results — inode metadata.  | `cache.attr_ttl`         | 5 m     |
| **dir**   | `readdir` listings.                  | `cache.dir_ttl`          | 5 m     |
| **data**  | File content, chunked.               | (no TTL — invalidation-driven) | — |
| **neg**   | "this path doesn't exist" cache.    | `cache.negative_ttl`     | 30 s    |

The negative cache is **memory-only** and is not counted against the byte budgets.

Data lives in fixed-size **chunks** (`cache.chunk_size_bytes`, default 1 MiB). A read at a non-aligned offset spans two chunks; the chunk size matches the streaming frame size so a chunk fetch maps 1:1 to one `Read` RPC.

The cache is **enabled by default**. Turn it off in `client.yaml`:

```yaml
cache:
  enabled: false
```

See [Client configuration](../client/config.md) for every knob.

## Push invalidation

The cache isn't pure-TTL — it also subscribes to a server-side stream of change events. This is on by default (`cache.subscribe_enabled`, default `true`); set it to `false` to fall back to pure TTL.

- The client opens a long-lived `Subscribe` RPC on the **fs** service (`RpcFs`).
- When the server applies a write that affects a path (locally on the server, or via another mounted client), it broadcasts a small invalidation event.
- Subscribers evict the matching attr / dir / data chunk on receipt.

This is what makes the cache **safe across multiple clients**. Two clients mounted on the same volume see each other's writes promptly without either polling.

### Freshness model

The cache starts **unverified** and revalidates every read against the server until the first Subscribe `HEARTBEAT` marks it globally verified. Once verified, TTL is the sole freshness signal — reads are served from cache until the entry ages out or an invalidation arrives. If `cache.subscribe_enabled` is `false`, the cache is marked globally verified at construction, so it runs TTL-only from the start.

## Close-to-open consistency

gMountie targets **close-to-open** consistency (the NFS model): when one client closes a file, subsequent opens on any client see the new bytes.

In practice:

- A write completes locally, gets coalesced if eligible (see [Client config — Write Coalescing](../client/config.md#write-coalescing)), and is flushed on `Flush` / `Fsync` / `Release`.
- On `Release`, the server has the new bytes durable enough that the next `Open` from any client returns them.
- Concurrent writers to the same file from different clients are *not* CRDT-merged — last writer's `Release` wins. gMountie is not multi-master.

## Read path — what a `cat` actually does

A `cat /mnt/shared/foo` on a warm cache:

1. **Lookup** → attr cache hit → no RPC.
2. **Open** → cache doesn't gate Open; one `file.Open` RPC opens an fd on the server.
3. **Read** loop — for each chunk:
   - Cache hit → memory copy, no RPC.
   - Cache miss → one `file.Read` streaming RPC; the chunk is cached on the way back.
4. **Release** → one `file.Release` RPC.

On a **cold** cache, step 1 is also one RPC and step 3 is all RPCs — that's the cold-`readdir`/cold-read cost. Warm, the only required RPCs are `Open` and `Release` plus any miss in step 3.

## Sequential reads — readahead skips the cache miss

Hot caches help re-reads. **Readahead** helps first reads of sequential files: after a few in-order reads, the client speculatively fetches the next N chunks in parallel, so the subsequent reads land in cache before the syscalls arrive.

The defaults (1 MiB chunks, window of 4) target ~50 ms RTT. Tune in `rpc.readahead_*` — see [Client config — Readahead](../client/config.md#readahead).

## Turning the cache off

Set `cache.enabled: false` (the cache is enabled by default). Every operation crosses the wire — useful for debugging consistency questions or measuring cold-path performance.

## Go deeper

- [Design — Caching & Consistency](../design/caching-and-consistency.md) — the eviction model, the chunk layout, the consistency proofs, edge cases (mixed-client races, partial writes, the negative-cache lifecycle).
- [Wire protocol](./wire-protocol.mdx) — what `Subscribe` and `Read` actually look like on the wire.
